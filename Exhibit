# Combine existing column names with the values from the first row
new_columns = [f"{col} {df.iloc[0, idx]}" for idx, col in enumerate(df.columns)]
df.columns = new_columns

# Remove the first row and reset the DataFrame index
df = df[1:].reset_index(drop=True)
